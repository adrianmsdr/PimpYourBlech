@page "/"
@using System.Text.Json
@using PimpYourBlech_BlazorApp.Components.Icons
@inject NavigationManager Nav


<PageTitle>PimpYourBlech</PageTitle>


<div class="hero-bleed">
    <div class="header-wrapper">
        <img src="HeaderMainMenu.png" class="header-image">
    </div>
</div>


<div class="home-section">
    <div class="main-row">
        <div class="our-partners-box">
            <img src="unsere-partner.png" class="our-partners">
        </div>

        <div class="menu-container">
            <button class="buttons car-button" @onclick="StartProgramm">
                <span class="car-icon">
                    <DrivingCarIcon />
                </span>
                <span class="button-text">
                    Zeit zu pimpen!
                </span>
            </button>

            <div class="button-column">
               
                <img class="video" src="/PYB-Video.webp" alt="" />
            </div>
        </div>
        
        <div class="funfact-card">
            <div class="funfact-title">Heute vor @yearsAgo Jahren:</div>

            @if (isLoading)
            {
                <div class="funfact-text">Lade…</div>
            }
            else
            {
                <div class="funfact-text">@factText</div>

                @if (!string.IsNullOrWhiteSpace(factUrl))
                {
                    <a class="funfact-link" href="@factUrl" target="_blank">
                        Mehr dazu
                    </a>
                }
            }
            <button class="buttons funfact-btn" @onclick="LoadOnThisDay">
                Neuer Fakt
            </button>
        </div>
    </div>
</div>


@code
{
    // Wichtig: Wikipedia REST API blockt gerne Requests ohne User-Agent -> sonst immer Fallback
    private static readonly HttpClient http = CreateHttp();

    private bool isLoading = true;
    private string factText = "";
    private string factUrl = "";
    private int yearsAgo;

    private static HttpClient CreateHttp()
    {
        var c = new HttpClient();
        c.DefaultRequestHeaders.UserAgent.ParseAdd("PimpYourBlech/1.0 (Blazor; localhost)");
        c.DefaultRequestHeaders.Accept.ParseAdd("application/json");
        return c;
    }

    protected override async Task OnInitializedAsync()
    {
        await LoadOnThisDay();
    }

    private async Task LoadOnThisDay()
    {
        isLoading = true;
        factText = "Lade…";
        factUrl = "";
        yearsAgo = 0;
        StateHasChanged();

        var today = DateTime.Today;
        var url = $"https://de.wikipedia.org/api/rest_v1/feed/onthisday/events/{today:MM}/{today:dd}";

        try
        {
            using var resp = await http.GetAsync(url);
            var body = await resp.Content.ReadAsStringAsync();

            if (!resp.IsSuccessStatusCode)
            {
                factText = $"Wikipedia API Fehler: {(int)resp.StatusCode} {resp.ReasonPhrase}";
                factUrl = "";
                yearsAgo = 0;
                isLoading = false;
                return;
            }

            using var doc = JsonDocument.Parse(body);
            var events = doc.RootElement.GetProperty("events");

            var count = events.GetArrayLength();
            if (count == 0)
            {
                factText = "Keine Ereignisse für heute gefunden.";
                factUrl = "";
                yearsAgo = 0;
                isLoading = false;
                return;
            }

            // Random Event picken
            var idx = Random.Shared.Next(count);
            var ev = events[idx];

            var text = ev.GetProperty("text").GetString() ?? "";
            var year = ev.GetProperty("year").GetInt32();

            yearsAgo = today.Year - year;
            factText = $"{year}: {text}";

            // Optional Link (wenn vorhanden)
            if (ev.TryGetProperty("pages", out var pages) && pages.GetArrayLength() > 0)
            {
                var p0 = pages[0];
                if (p0.TryGetProperty("content_urls", out var cu) &&
                    cu.TryGetProperty("desktop", out var desk) &&
                    desk.TryGetProperty("page", out var page))
                {
                    factUrl = page.GetString() ?? "";
                }
            }

            isLoading = false;
        }
        catch (Exception ex)
        {
            yearsAgo = 0;
            factText = $"Exception: {ex.GetType().Name} – {ex.Message}";
            factUrl = "";
            isLoading = false;
        }
    }
    
    public void StartProgramm()
    {
        Nav.NavigateTo("/startMenu");
    }
    
}
<style>

    .main-row {
        display: flex;
        justify-content: center;
        align-items: stretch;
        gap: 24px;
    }

    .header-wrapper {
        margin-top: 16px;
        width: 100%;
        overflow: hidden;
        background: rgba(0, 0, 0, 0.25);
        backdrop-filter: blur(8px);
        /* border-radius: 18px;
            border: 1px solid rgba(255, 0, 0, 0.25);
            box-shadow: 0 8px 25px rgba(0, 0, 0, 0.6);
    */
        border-radius: 12px;
        border: 2px solid #ff1a1a;

        box-shadow:
            inset 0 0 0 1px rgba(255,255,255,0.08),
            0 6px 16px rgba(0,0,0,0.6);


    }

    .header-image {
        width: 100%;
        max-height: 320px;
        display: block;
        object-fit: cover;
        object-position: center 12%;
    }

    .button-column {
        width: 100%;
        display: flex;
        flex-direction: column;
        gap: 14px;
        align-items: center;
    }

    .video {
        width: 100%;
        aspect-ratio: 16 / 9;
        object-fit: cover;
        border-radius: 22px;
        box-shadow: 0 25px 60px rgba(0, 0, 0, 0.65);
    }

    .buttons {
        width: 100%;
        height: 45px;
        font-family: Bahnschrift;
        font-size: 1.1rem;
        color: white;
        background-color: #7a0000;
        border-radius: 12px;
        border: 2px solid #ff1a1a;
        box-shadow: 0 6px 16px rgba(0, 0, 0, 0.6);
        cursor: pointer;
        transition: transform 0.25s ease, background-color 0.25s ease;
    }

    .buttons:hover {
        transform: scale(1.05);
        background-color: #b30000;
    }

    .car-button {
        position: relative;
        display: flex;
        align-items: center;
        justify-content: center;
        overflow: hidden;
        gap: 10px;
        transition: background-color 0.35s ease, box-shadow 0.35s ease;
    }

    .car-icon {
        position: absolute;
        left: 18px;
        opacity: 0;
        transform: scale(0.8);
        display: flex;
        align-items: center;
        transition:
            left 0.45s ease,
            transform 0.45s ease,
            opacity 0.3s ease;
    }

    .button-text {
        transition: opacity 0.35s ease;
    }

    .button-text:hover {
        opacity: 0;
    }

    .car-button:hover {
        background-color: #5a0000;
        box-shadow: 0 10px 22px rgba(0, 0, 0, 0.8);
    }

    .car-button:hover .car-icon {
        left: 50%;
        transform: translateX(-50%) scale(1);
        opacity: 1;
    }

    .car-button:hover .button-text {
        opacity: 0.2;
    }

    .our-partners {
        width: 100%;
        height: 100%;
        object-fit: contain;
    }

    .our-partners-box {
        width: 280px;
        height: 550px;
        display: flex;
        justify-content: center;
        align-items: center;
        padding: 12px;
        overflow: hidden;
        background: rgba(0, 0, 0, 0.25);
        backdrop-filter: blur(8px);
        border-radius: 12px;
        border: 2px solid #ff1a1a;
        box-shadow:
            inset 0 0 0 1px rgba(255,255,255,0.08),
            0 6px 16px rgba(0,0,0,0.6);
    }

    .funfact-card{
        width: 280px;
        padding: 14px;
        background: rgba(0,0,0,0.25);
        backdrop-filter: blur(8px);
        border-radius: 12px;
        border: 2px solid #ff1a1a;
        box-shadow:
            inset 0 0 0 1px rgba(255,255,255,0.08),
            0 6px 16px rgba(0,0,0,0.6);
    }
    .funfact-title{ font-family: Bahnschrift; color: white; font-size: 1.05rem; margin-bottom: 10px; }
    .funfact-text{ color: rgba(255,255,255,0.9); line-height: 1.35; margin-bottom: 10px; }
    .funfact-link{ color: #ff7a7a; text-decoration: none; display: inline-block; margin-bottom: 10px; }
    .funfact-btn{ height: 40px; }

    
    .home-section{
        margin-top: 16px;
        margin-bottom: 16px/* exakt unter dem Header-Radius */
    }

    .menu-container,
    .our-partners-box,
    .funfact-card{
        margin-top: 0 !important;
    }
    
</style>